import { createContext } from "react";

export const Languages = {
    EN: "EN",
    PL: "PL"
}

export const LanguageContext = createContext("no provider");

export const LOCALSTORE_LANGUAGE = "language";
